// import { DFS } from '../../algorithms/DFS';

export function startPathfinding() {
  return new Promise((resolve) =>
    setTimeout(() => {
      // const result = DFS(byId, startNodeId, targetNodeId, animatedNodes);
      return resolve({ result: 'data' });
    }, 0),
  );
}
